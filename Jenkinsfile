pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh 'echo "HEllo from Jenkins"'
                sh 'whoami'
            }
        }
    }
}